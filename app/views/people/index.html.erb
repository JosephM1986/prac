  <h1>People Listings</h1>

    <table>
      <tr>
        <th>First Name</th>
        <th>Last Name</th>

        <th></th>
        </tr>

        <% @people.each do |people| %>
        <tr>
          <td><%= people.first_name %></td>
          <td><%= people.last_name %></td>
          <td><%= link_to 'show', person_path(people) %></td>
        </tr>
        <% end %>
      </table>

<%= link_to 'People listing', controller: 'people' %>
<%= link_to 'New Person', new_person_path %>
